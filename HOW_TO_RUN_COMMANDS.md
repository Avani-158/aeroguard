# Commands to Run the Flutter App

## 🚀 Main Command

### Run on Chrome (Web):
```powershell
cd "D:\iot air purifier"
flutter run -d chrome
```

### Run on Edge (Web - Alternative):
```powershell
cd "D:\iot air purifier"
flutter run -d edge
```

### Run on Windows Desktop:
```powershell
cd "D:\iot air purifier"
flutter run -d windows
```

## 📋 Step-by-Step

1. **Open PowerShell or Terminal**
2. **Navigate to project:**
   ```powershell
   cd "D:\iot air purifier"
   ```

3. **Run the app:**
   ```powershell
   flutter run -d chrome
   ```

## ⏱️ What to Expect

- **First time:** Takes 1-3 minutes to compile
- **Subsequent runs:** Faster (30-60 seconds)
- **Chrome will open automatically** when ready
- **Or look for URL in terminal** like: `http://localhost:xxxxx`

## 🔍 Check Available Devices

To see what devices you can run on:
```powershell
flutter devices
```

## 🛠️ Other Useful Commands

### Clean and Rebuild:
```powershell
flutter clean
flutter pub get
flutter run -d chrome
```

### Build for Web (without running):
```powershell
flutter build web
```

### Check for Errors:
```powershell
flutter analyze
```

## 💡 Quick Reference

| Command | What It Does |
|---------|-------------|
| `flutter run -d chrome` | Run app in Chrome browser |
| `flutter run -d edge` | Run app in Edge browser |
| `flutter run -d windows` | Run as Windows desktop app |
| `flutter devices` | List available devices |
| `flutter clean` | Clean build files |
| `flutter pub get` | Install dependencies |

## 🎯 Most Common Command

```powershell
cd "D:\iot air purifier"
flutter run -d chrome
```

This is the command you'll use most often!


